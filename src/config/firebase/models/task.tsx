export default (id: number, task: string, time:string, description: string, timestamp: string) => ({
    id: id,
    task: task,
    time: time,
    description: description,
    timestamp: timestamp
});